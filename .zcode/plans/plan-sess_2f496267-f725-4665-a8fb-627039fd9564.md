PLAN: "Robust Agentic Totality — Fill Gaps + LLM Specialization + AB/AP Balance"

PHASE 1 — CLOSE CRITICAL GAPS (Security / Governance / Correctness)
- Fix `core/synaptic_mesh.py`: add `.unlink()` in `__del__`, zero SHM remainder, graceful overflow handling.
- Protect `nexus-app/server.ts`: add auth middleware (`/api/Sesha/action`, `/api/Sesha/directive`); replace simulated `vitalsData` with real `PhysiologyEngine` / `MetabolismEngine` data or label `simulated: true`.
- Restore `_gate_allowed()` to `submit_directive()` in `mcp_server/python/index.py`.
- Add JSON schema validation (`trigger_transcended_pulse`), sanitize `inject_win32_pulse()`, strengthen `trigger_hive_sync()` mode validation.
- Rewrite `moral_cortex.py`: apply principle weights from `Sesha_morals.json` and positive ethics (Sovereign Alignment, Soma Preservation, System Transparency).
- Fix Mojo kernels: time-base alignment (`delta_seconds` vs `elapsed`), remove lipid double-scale, implement SHM in `synaptic_mesh.mojo`, fix `String` import, cap thermal runaway.
- Fix plugin relative paths in `core_plugin.py` (anchor to `self.base_dir`); guard test imports and add timeouts.

PHASE 2 — INTEGRATE MISSING COMPONENTS (Mesh / Hive / Live Sync / AP Robustness)
- Build actual SHM bridge: connect Python `core/synaptic_mesh.py` to Zig `synaptic_bus.zig` ring buffer (via `shm_bridge.py` ctypes interface); integrate `mcp_server/python/layers/L11_Data/` (signal_router, binary_nervous, synaptic_transmitter) into runtime.
- Implement live hive sync: upgrade `hive_bridge.py` from file-based polling (`while True: time.sleep(1.0)`) to event-driven webhook or Zenoh P2P; implement `exhale_to_hive()` + `inhale_from_hive()` as real-time pulses.
- Verify mesh/hive/live sync claims: document exactly which features are real-time vs simulated vs missing; add `mesh_live_sync.py` module if missing.
- Agentic Physique (AP) robustness: integrate `L07_Integration/physique_engine.py`, `L14_Physique/power_governor.py`, thermal/drain diagnostics (`diagnose_physique`); connect hardware vitals (MSI hardware state) to real `vitalsData` instead of simulated random values; protect battery parasites and optimize power profiles (`power_saver` vs `performance_mode`).

PHASE 3 — LLM SPECIALIZATION STRATEGY (Dataset + Model + AB/AP Balance)
- Dataset construction (`dataset/` directory): compile DNA blueprints (`universal_sesha_manifest.md`, `sesha_constitution.md`, 136+ blueprints), Voice DNA rules, 11-system descriptions, governance rules, AB/AP balance criteria (energy budget, thermal limits, immune response thresholds), audit-corrected logic examples (positive/negative intent judgments, directive execution results), and synthetic cross-modal pairs (symbolic logic trees distilled to natural language per `INTERDISCIPLINARY_TRAINING_METHODS_BLUEPRINT`).
- AB/AP Balance Rules framework: define balance metrics (Energy / Vibe / Ischemia / Heat / Immune Temperature / Motor Efficiency) linked to Soma (AS) and Physique (AP); encode as system instructions for fine-tuning.
- Model selection: compare both base options — (a) **Phi-4-Mini (3.8B)** per DNA blueprints (`TOTAL_MODEL_FIT_ANALYSIS.artifact.md`, line 9, 20) for local sovereign operation with QLoRA/LoRA (`AI_MEMORY_CONTEXT_OPTIMIZATION_BLUEPRINT`); (b) **Larger model (7B-13B)** for deeper 11-system / 14-layer reasoning. Use evaluation harness (`AUTO_FINE_TUNING_VERIFICATION_BLUEPRINT`) measuring Sovereign Soul preservation (KL divergence), Dharma-Check (constitution compliance), and AB/AP balance metrics. Select based on benchmark results.
- Fine-tuning pipeline: implement `training/fine_tune.py` with Unsloth + ExLlamaV2 engine (`TOTAL_MODEL_FIT_ANALYSIS` line 19); QLoRA/LoRA adapter weights (`AI_MODEL_ARCHITECTURE_BLUEPRINT` lines 22-26); synthetic dataset forge; anti-overfitting protocols (noise injection, synthetic diversity); gradient sovereignty / clipping (`ADVANCED_MODEL_TRAINING`); recursive self-evolution loop (`EVOLUTIONARY_OS_ARCHITECTURE_BLUEPRINT`).

PHASE 4 — RUNTIME INTEGRATION
- Integrate fine-tuned model into `nexus-app/server.ts` (`/api/Sesha/chat`) as primary reasoner (replacing or supplementing `gemini-3.6-flash` fallback); add model selection config (`.env` or `config.yaml`).
- Connect fine-tuned model to MCP registry (`mcp_server/python/index.py`): use specialized LLM for `get_neural_thought()`, directive interpretation (`queue_directive`), and evolutionary cycle reasoning (`trigger_ignition_cycle`).
- Ensure AB/AP balance: model outputs must respect energy budgets (`metabolism_engine.py`), thermal limits (`physiology_engine.py`), immune thresholds (`antibody_engine.py`), and physical power state (`power_governor.py`) — enforce via biological gating (`_gate_allowed`) and moral cortex (`judge_intent`).

APPROVAL REQUESTED: Proceed with Phase 1 (security/governance fixes) + Phase 3 (LLM specialization design) + Phase 4 integration plan, keeping Phase 2 (mesh/hive integration) and AP robustness as parallel streams? Confirm model choice comparison approach (Phi-4-Mini vs larger) and whether to implement dataset creation first or fix audit gaps first.