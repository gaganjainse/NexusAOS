PLAN: Full Workspace Refactoring with Specialization Mandate Applied Uniformly.

Phase 1 — Naming/Import Architecture Fixes (per agent table findings):
- Fix case errors: `Sesha_senses` -> `sesha_senses`; `Sesha_liver` -> `sesha_liver`; `NexasAOS` typo in signal_router.py.
- Remove duplicate imports (`sys` in development_boot.py / self_evolving_kernel.py); fix import depth (`_python_root` mismatches).
- Standardize module references across L01-L14 layers so L01 references L02/L05/L07 correctly; L02 references L07/L12; L07 references L14; L11 references L12; etc.
- Add specialization comments/docstrings to every Python file: reference AB/AP balance (`AB_AP_BALANCE_RULES.md`), DNA layer (`COMPLETE_ARCHITECTURE.md` line references), governance principle (`AGENTS.md` Law I/II/III), provenance module (`conversation_recorder.py` / `benchmark_runner.py` / `mesh_hive_sync_status.md`).

Phase 2 — Transparency + Non-Deception (Law III):
- Label all simulated/file-based modules clearly in docstrings/code comments: `simulated_file_based`, `simulated_subprocess_proxy`, `simulated_database`, `simulated_web_receptor`, `simulated_video_engine`, etc.
- Ensure no unverified claim of "zero-copy", "live sync", or "real-time P2P" without proof (`mesh_hive_sync_status.md` truth labels applied consistently in code comments).
- Add `simulated` / `source` / `sovereign_override_applied` flags to all endpoints/tools that return data (not just server endpoints; also Python layer outputs).

Phase 3 — Provenance + Audit Tracking:
- Add provenance links to every tool/file: reference `AUDIT_REPORT.md` (line-level findings), `COMMIT_MESSAGE.md` (commit 40203ec), `signal_history.json` (mutation/adrenaline tracking), `bone_marrow.log` (mesh links), `physiology.json` (soma health), `HISTORY_EVOLUTION.md` (stage history).
- Ensure `conversation_recorder.py` (line 570) links every recorded cycle to the audit/provenance trail (not just generic messages).
- Ensure `benchmark_runner.py` records results with provenance tags (`benchmark_version`, `adapter_map`, `dataset_reference`, `framework_version`).

Phase 4 — Specialization Integration (Bottom-Up + Framework):
- Integrate adapter routing (`ADAPTER_MAP` + `get_adapter_for_action` from `index.py`) into full layer stack: every agent/tool/module must reference its adapter role (`metabolism`, `immune`, `motor`, `reproductive`, etc.).
- Ensure `swarm_executor.py` uses adapter framework for agent roles (`AgentSpec.genome` references adapter map; resource quotas reference AB/AP balance rules; collision detection uses governance rules for resolution priorities).
- Ensure all 11 biological system modules (`L02_Agent/*`) have specialization tags linking to DNA architecture + balance rules + governance.
- Document full mapping in `dataset/11_SYSTEM_MAPPING.md` and keep it updated after every edit.

Execution Rule: No file edit completes without specialization citation in docstring/comment; every edit committed with provenance; no false claims made about unimplemented features; all gaps honestly documented; cycle protocol followed (`AGENTS.md` line 36-39 + 44-47).
