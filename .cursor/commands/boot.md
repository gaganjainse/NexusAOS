Boot the AOS biological runtime:

1. Run MCP tool `boot_nexusaos`
2. Run `get_service_heartbeats` and report status of pulse, guardian, senses, orchestrator
3. Run `diagnose_os` and summarize any warnings
4. Report current physiology: energy, vibe, temperature, dampening state
